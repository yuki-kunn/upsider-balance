{
  "name": "upsider-balance",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter @upsider-balance/shared build && pnpm --filter web build",
    "emulators": "firebase emulators:start --only firestore,storage,auth",
    "create-account": "pnpm --filter @upsider-balance/shared build && tsx scripts/create-account.ts"
  },
  "devDependencies": {
    "@upsider-balance/shared": "workspace:*",
    "firebase-admin": "^12.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
